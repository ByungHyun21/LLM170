#version 450
layout(local_size_x = 64) in;
layout(binding = 0) buffer Out { float o[]; };
layout(push_constant) uniform PC { uint n; float scale; float bias; } pc;
void main() {
    uint i = gl_GlobalInvocationID.x;
    if (i >= pc.n) return;
    o[i] = float(i) * pc.scale + pc.bias;
}
