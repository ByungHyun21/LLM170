[workspace]
resolver = "3"
members = [
    "crates/gguf",
    "crates/profiler",
    "crates/server",
]

[workspace.package]
version = "0.1.0"
edition = "2024"

[profile.release]
# 배포 빌드. 프로파일러 계측은 debug_assertions 기반이라 release에서 zero-cost.
# (성능 튜닝 단계에서 lto/codegen-units 조정 여부 재검토)
